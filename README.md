# 0mniPING
A modular, multi-layer network observability &amp; diagnostic suite written in Go. It delivers precise, asynchronous probing across Layer 2 links, Layer 3 networks, Layer 4 transport protocols, Layer 7 applications, DNS resolution, and virtual tunnels, providing real-time console streaming, structured JSON logs, &amp; Prometheus metrics endpoints
